import React from 'react'

const Filter = () => {
    return (
        <div>
            {/* get passed item you want to filter
                merge tools and languages array and filter based on that
                keep seperate array so you can stack filtered items */}
        </div>
    )
}

export default Filter;
